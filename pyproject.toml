[project]
name = "kaggle-llm-classification"
version = "0.1.0"
description = ""
authors = [
    {name = "Stefano Laptop",email = "s.xxx@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "pandas (>=2.3.2,<3.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "toolz (>=1.0.0,<2.0.0)",
    "transformers (>=4.56.2,<5.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
test = [
    "isort (>=6.0.1,<7.0.0)",
    "flake8 (>=7.3.0,<8.0.0)",
    "flake8-docstrings (>=1.7.0,<2.0.0)",
    "pre-commit (>=4.3.0,<5.0.0)"
]
dev = [
    "matplotlib (>=3.10.6,<4.0.0)",
    "seaborn (>=0.13.2,<0.14.0)"
]
